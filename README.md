# Atlas — Personal Learning Codex

**Live: https://techeese.github.io/learn-atlas/**

A gamified, self-hosted study site for the topics you're learning alongside a full-time job:
**Linear Algebra, Calculus, Algorithms, Deep Learning, Reinforcement Learning, LLMs, and Probability & Statistics**.

Currently **148 lessons · 2,368 fact-checked MCQs · 889 flashcards · 442 homework problems · 343 worked
examples · 77 interactive visualizations**, each lesson with rendered math, step-by-step worked examples, and
spaced-repetition cards — wrapped in an XP / level / streak / mastery / achievement system designed to make
hard ideas *click* and *stick*.

Zero build step (pure HTML/CSS/JS, all progress in `localStorage`); auto-deploys to GitHub Pages on push.

## Features

- **Lectures** — readable lessons with KaTeX-rendered math, callouts, and code, each opening with an auto-generated **"On this page"** table of contents that jumps to any section and **highlights the section you're currently reading** as you scroll (every lesson has 4–11). A slim **reading-progress bar** at the top of the screen fills as you move through any long page, so you always know how far you've come (and how much is left), and a **back-to-top button** fades in once you've scrolled down a screen — one tap returns you to the top of a long lesson, the Lab, or the Hall of Achievements. Leave a long lesson half-read and Atlas remembers where you were: reopen it and a one-tap **"⤓ Resume where you left off"** pill jumps you straight back to your spot (no jarring auto-scroll — you choose when to jump). Each lesson also carries an **in-module navigator** — a strip of dots (one per lesson in the current module) that shows which you've completed, which you're on, and what's left, and lets you **jump to any lesson in the unit in one tap** — plus the module name in the breadcrumb and a live "lesson X / N" position, so you always know where you are within a unit.
- **Examples** — an optional per-lesson tab of worked examples with revealable working.
- **"Deeper dive" expandables** — for genuinely hard concepts, an inline collapsible offers an *alternative* explanation or extra intuition (e.g. Bayes recast as natural frequencies; the reparameterization trick as a pathwise derivative; cross-entropy as a KL divergence to the true distribution; dynamic programming as nothing but memoized recomputation; a dynamic array's `O(1)` append explained by amortized accounting; reading `n log n` straight off the recursion tree; why attention divides by `√dₖ`; why the KV-cache makes generation linear-per-token; how DPO drops the reward model entirely; why value iteration always converges (the Bellman operator is a contraction); bootstrapping and the bias–variance trade between TD and Monte Carlo; on-policy vs off-policy at the cliff's edge; the SVD as rotate-stretch-rotate; how the four fundamental subspaces interlock; the chain rule as the engine of backprop; why the gradient is steepest ascent perpendicular to the level sets; conditional expectation as the best predictor — what regression learns; the LLN vs the CLT and the √n shrinkage of error; why a zero determinant means non-invertible and why det(AB)=det(A)det(B) follow from "determinant = volume-scaling"; why backprop runs *backward* — the cost asymmetry of autodiff; the Bellman equation as a contraction fixed-point that guarantees convergence; the Chinchilla compute-optimal correction — why a smaller, longer-trained model can win; what "95% confidence" actually means — the procedure, not the interval; Adam decomposed into momentum + RMSProp + bias correction; why PPO *clips* — the pessimism that stops a policy update from self-destructing; when greedy is provably optimal — the exchange argument (and why 0/1 knapsack breaks it); why convexity makes optimization easy — every local minimum is global; why "independent" and "uncorrelated" are not the same thing, with the $Y=X^2$ counterexample; a Transformer block as just "mix across tokens (attention), then think per token (MLP)" over a residual stream; BPE tokenization as greedy compression — why models can't spell; Monte Carlo vs TD as "wait for the truth" vs "trust your own guess"; what a p-value is *not* — the reversed conditional / prosecutor's fallacy; PCA as nothing but the SVD of centered data; max-flow = min-cut, why the bottleneck *is* the answer; the Hessian's eigenvalues as the min/max/saddle test; a symmetric matrix as a pure stretch along perpendicular axes (the spectral theorem); DQN's replay + target-network as fixes for the two ways deep Q-learning self-destructs; why attention is order-blind until positional encoding puts the sequence back; a GAN as a two-player minimax game (and why that makes it unstable); the Poisson distribution as the law of rare events (the binomial limit); Kruskal's MST as greed made safe by the cut property, with Union-Find as the cheap cycle test; LoRA as a low-rank patch — fine-tuning under 1% of the weights; attention as a soft, differentiable dictionary lookup (query·key → softmax → weighted values); an improper integral as a limit in disguise (why `1/x²` converges but `1/x` diverges); actor-critic as REINFORCE with a variance-cutting baseline (act on the advantage); why you almost never actually compute a matrix inverse — you solve `Ax=b` by elimination instead; KMP string search never re-reads the text — the failure function remembers; why LLMs hallucinate — fluency optimizes for plausible, not true; why sample variance divides by `n−1` — Bessel's correction is the price of estimating the mean; a diffusion model as just learning to denoise; why LSTMs remember and vanilla RNNs forget — additive cell-state vs. the multiplicative recurrence that vanishes gradients; a differential equation as a slope field you thread a curve through; backtracking as depth-first search that prunes branches that can't possibly work; and why an RL agent must explore against its own knowledge — uncertainty as the thing UCB spends on; why NP-complete problems stand or fall together — one fast algorithm would crack them all; related rates as the chain rule wearing a clock; why in-context learning isn't really learning — the prompt conditions, it doesn't teach; and hypothesis testing as proof by contradiction with a dial; why orthonormal bases are computationally golden — Qᵀ=Q⁻¹, so coordinates are just dot products; regularization as a vote for simpler explanations; RAG separating what a model knows from how it reasons; and why gradient descent zigzags — the condition number κ mismatching curvature across directions; the exponential distribution being memoryless — a wait with no aging; model-based RL letting you plan against a learned model instead of only reacting; why balanced splits make divide-and-conquer fast — a shallow tree of cheap merges; and the integration tricks as derivative rules run backward — substitution is the chain rule reversed, by-parts the product rule; why LLM training needs warmup and decoupled weight decay; how pooling buys translation invariance and a wider receptive field; choosing the right guarantee when exact-and-fast is impossible — approximation vs randomized; and probability as set theory with a measure; why offline RL fights distribution shift — don't trust value estimates where you have no data; least squares as a literal orthogonal projection — the residual is perpendicular to the columns; the Intermediate Value Theorem guaranteeing a root exists before you find it; and why pretrained features transfer — general representations are task-agnostic; instruction tuning as behaviour-shaping not knowledge (same next-token objective, demonstration data); a loop invariant as induction in disguise; a random variable as a function from outcomes to numbers; and why the +C — differentiation discards constants, so antidifferentiation recovers a whole family; embeddings turning symbols into learned geometry; policy iteration as a monotone evaluate-then-improve climb to optimal; the Jacobian as the derivative generalized to vector functions; and a partial derivative as freezing every axis but one; a transformer block as communicate-then-compute wrapped in residuals; union-find's near-O(1) cost from laziness plus flattening; correlation as covariance with the units divided out; and matrix multiplication as function composition; convolution as weight-sharing plus locality; the order to debug a training run; temperature/top-k/top-p as three knobs on one coherence-vs-diversity trade-off; and why Dijkstra needs non-negative edges; why depth without nonlinearity collapses to a single matrix multiply; the deadly triad behind off-policy value-approximation divergence; the binary-search invariant and the off-by-one/overflow bugs that ignore it; and why the normal distribution is everywhere — CLT, maximum entropy, and convenience; one neuron as logistic regression and an MLP as a universal approximator; why Big-O discards constants and lower-order terms; how expectation always adds but variance carries a covariance correction; and the definite integral as a limit of rectangles; TD(λ) as a single dial between TD and Monte Carlo; quicksort vs mergesort — same Θ(n log n), different trade-offs; alignment as a proxy problem (Goodhart, reward hacking, sycophancy); and how the first and second derivatives draw a function's whole shape; BFS vs DFS as nothing but a queue vs a stack; why learning-rate schedules warm up then decay; where the binomial formula comes from (one ordering × how many orderings); and why L'Hôpital's rule works — it's a ratio of tangent-line slopes; arrays vs linked lists as a memory-layout tradeoff; why ∇f=0 is necessary but not sufficient for an optimum; why behavioural cloning drifts via compounding errors; and backprop as the chain rule run in the cheap matrix-multiplication order) — kept out of the main flow until you want it. Every subject carries several (100+ across the codex).
- **Quizzes** — instant feedback + explanations; earns XP, perfect-score bonus; the result screen lands with a count-up score reveal (and confetti on a flawless run), then keeps your momentum with one-tap **Redrill the N you missed** (mastery mode — re-queues each wrong question until it sticks) and a **Next lesson →** button, so finishing a quiz flows straight into the next thing instead of dead-ending on a lone "retry."
- **Quick Check** — a low-stakes retrieval prompt waiting at the end of every lecture: *"reading it is not the same as remembering it."* Three questions drawn from the lesson's own bank, with instant feedback — **no grade, no XP**, just to see if it stuck, then a one-tap jump to the full quiz. Brings the *testing effect* into the reading flow for the many learners who read and move on without ever switching to the quiz tab.
- **Flashcards** — SM-2-style spaced repetition. A global **Daily Review** serves the cards that are genuinely *due* for review, plus a capped trickle of brand-new cards each session (so a fresh deck of 800+ never lands on you all at once). The **new-cards-per-session cap is yours to set** (5–100, in Settings) — crank it up to learn faster or dial it down to keep sessions light.
- **Recall ("explain it back")** — a free-recall study mode per lesson: write the whole concept from a blank page, then self-check against the lesson's key points and score yourself. Active recall done right — it feeds mastery and earns XP. (The Feynman technique, built in.)
- **Homework** — practice problems with progressive hints and full solutions.
- **Daily Mix** — one click assembles a short guided session: due flashcards → a weak-spot quiz → **redeem a few of your past mistakes** → a "learn next" lesson, with a step indicator. Adapts to skip phases with nothing to do. The "open the app, study for 15 minutes" button.
- **"Keep it fresh"** — concept mastery *decays over time* (a 45-day half-life), and the dashboard surfaces the concepts you once knew well that are now **fading** (slipping out of the "mastered" band) — the spacing-effect prompt to review *before* you forget, distinct from the "weak spots" you're still struggling with. One click — **↻ Quick refresh** — drills those exact concepts in mastery mode, and getting them right re-locks the knowledge and clears them from the list.
- **Visualization Lab — 77 interactive widgets**: drag vectors, morph space with a 2×2 matrix, eigenvectors, the derivative, gradient descent (1-D and a 2-D contour map), sorting, Big-O growth, activation functions, a neural-net forward pass, an attention heatmap, PCA, convolution & feature maps, RNN unrolling, a gridworld solved by value iteration, embedding arithmetic, Bayes & base rates, k-means clustering, an overfitting / bias–variance polynomial-fit demo, a backpropagation computational-graph walkthrough, a decoding playground (temperature + top-p nucleus), a Central Limit Theorem demo (watch sample means converge to a bell), a Normal-distribution explorer (μ/σ sliders with empirical-rule bands and live interval probabilities), a Covariance & Correlation scatter (drag ρ to tilt a point cloud, with best-fit line, covariance ellipse, and live sample correlation), a Confidence-Interval coverage simulator (draw many CIs and watch ~95% capture the true mean while ~5% miss), a Diffusion noising/denoising animation (structured data dissolves into Gaussian noise and reassembles), a Convex-vs-non-convex gradient-descent landscape (drop a ball: it finds the global minimum on a convex bowl but gets trapped in a local valley on a bumpy one), a Hypothesis-testing / p-value explorer (drag the observed statistic to see the p-value as a tail area against the rejection region), a Byte-Pair Encoding (BPE) trainer (step a tokenizer through merges — start from characters, merge the most frequent adjacent pair each press, and watch the vocabulary grow while the corpus token count shrinks), a Learning-Rate Schedule explorer (compare constant / step / exponential / cosine / linear schedules with warmup, peak, and floor, and watch the rate trace its path across training), and a Q-Learning Gridworld (watch an agent learn a policy from experience — it explores ε-greedily and updates Q-values from its own transitions; press Train and the greedy arrows snap toward the goal while avoiding the pit), a Beam-Search decoding tree (watch beam search keep the *k* most-probable partial sequences and beat greedy), a Cross-Entropy Loss & Perplexity explorer (slide the probability on the true class and watch −ln(p) and perplexity blow up), and an **Optimizer Race** (drop SGD, Momentum, RMSProp, and Adam on the same ill-conditioned loss valley and watch plain SGD zig-zag across the steep walls while momentum and the adaptive methods cut straight to the minimum), and a **Riemann Sum** visualizer (approximate the area under a curve with rectangles — pick the function and the left/right/midpoint rule, then add more rectangles and watch the sum converge to the exact definite integral while the over-/under-estimate error shrinks), and a **Taylor Polynomial** approximator (overlay a function and its degree-n Taylor polynomial about 0 — degree 1 is exactly the tangent-line linearization, and each added term widens the interval where the polynomial hugs the true curve before it peels away), and a **Dropout** visualizer (watch a small network drop a random fraction of its hidden units on every training pass — a different thinned sub-network each time, all sharing one set of weights — then switch to Test mode where every unit is kept and scaled, the deterministic approximation to averaging the whole ensemble), and a **Positional Encoding** explorer (self-attention is order-blind — see how sinusoidal encodings fix that: the iconic position×dimension heatmap, the stack of different-frequency sine waves behind it, and a relative-similarity view showing that nearby positions stay similar so attention can recover *relative* position), and a **KL Divergence** explorer (drag a Gaussian posterior and watch KL(q‖p) — the VAE's latent regularizer — shrink to exactly zero as q matches the standard-normal prior, while the side-by-side KL(p‖q) makes the famous asymmetry concrete: reverse KL is mode-seeking, forward KL is mode-covering), and a **TD vs Monte Carlo** prediction demo (the classic 5-state random walk — run episodes and watch TD(0), which bootstraps off its own next estimate every step, and Monte Carlo, which waits for each episode's actual return, both crawl toward the true values 1/6…5/6, with live RMS errors showing TD's lower variance), and a **Multi-Armed Bandit** explorer (five arms with hidden win-rates — pull them under ε-greedy, UCB, or pure-greedy and watch the estimates converge to the true rates while the cumulative-regret curve reveals each strategy's explore/exploit tradeoff: pure greedy gambles, ε-greedy pays a steady tax, UCB stays consistent), and a **Binomial ⇄ Poisson explorer** (drag *n* and *p* to reshape the Binomial PMF bar chart — watch the mean *np*, the variance, and the bell emerge — then overlay Poisson(*np*) and see it nearly coincide when *n* is large and *p* small: the law of rare events made visual), and a **GAN training** visualizer (watch the adversarial minimax game play out: a generator distribution slides onto the real data while the optimal discriminator D(x)=P(real) collapses to 0.5 everywhere — a coin flip — at equilibrium, then flip to a two-mode target to watch a single-Gaussian generator *mode-collapse* onto one peak while D stays confident over the abandoned mode), and a **Graph Traversal** visualizer (press play and watch a 9-node graph get explored — BFS rides a queue and fans out in rings, finding shortest paths in an unweighted graph, while DFS rides a stack and plunges deep down one branch; the live queue/stack contents and per-node visit order make the queue-vs-stack difference tangible, and you can click any node to start from it), and a **Fundamental Theorem of Calculus** visualizer (sweep a point *x* and watch the signed area under *f* pile up into the accumulation function A(x)=∫f in a second panel — where *f* is tall A climbs fast, where *f*=0 A is flat, where *f*<0 A falls — with a tangent line whose slope always equals the height *f(x)*, making A′(x)=f(x) literally visible: the two halves of calculus shown to be inverses), and an **Orthogonal Projection** visualizer (drag a target vector *b* and watch its shadow *p* land on a line — the closest point to *b* — while the error *e = b − p* stays locked at a right angle to the line, the normal equation aᵀe=0; the perpendicular drop that *is* least squares and linear regression, made draggable), and a **Hash Table** visualizer (insert keys and watch them scatter into buckets by h(k)=k mod m — chains colored by collision severity, a live load factor α=n/m and expected-lookup readout, and a "find" that reports how many comparisons it took; crank the table size or pile on keys and watch α climb and chains grow, the visceral "why O(1) needs a low load factor" story), and a **Binary vs Linear Search** race (the same sorted array searched two ways at once — binary halves its window each comparison and homes in within a handful of steps while linear crawls cell by cell, with live comparison counts making the O(log n) vs O(n) gap impossible to miss), and a **Discount Factor γ** explorer (drag γ and watch the weight γᵗ of a future reward decay geometrically — small γ is myopic, γ near 1 is far-sighted — with the effective horizon 1/(1−γ) and the discounted return updating live, the visceral feel for how far ahead an RL agent looks), and a **Signal-Propagation / Initialization** explorer (drag the per-layer gain g and watch a 24-layer network's signal vanish to nothing below g=1, stay flat at g≈1, or explode above it — the exponential-in-depth reason good initialization, normalization, and residual connections exist), and a **Causal Masking** visualizer (the attention triangle made literal — toggle the mask and watch a 7×7 attention matrix flip between full bidirectional (BERT-style, every token sees every token) and lower-triangular causal (decoder-LM, token *i* attends only to positions ≤ *i*, with ✕ on the blocked future cells); step through generation row by row to see why the whole matrix can be computed at once during teacher-forced training while inference fills one query row at a time — the structural reason the KV cache exists), and a **Dot Product & Angle** visualizer (drag two vectors and watch a·b = |a||b|cosθ come alive — the angle arc and the running number turn **sage when the angle is acute** (a·b > 0), **gold at a right angle** (a·b = 0, the ✕/⊥ orthogonality case), and **rust when obtuse** (a·b < 0), with b's scalar projection onto a drawn as a shaded bar that flips to point *backwards* once the angle passes 90° — the geometric meaning behind projections, cosine similarity, and attention scores), and a **Dynamic Programming** visualizer (watch the edit-distance (Levenshtein) table between *kitten* and *sitting* fill cell by cell — each cell copies its diagonal neighbour for free on a character match, otherwise it's 1 + the min of its top/left/diagonal; step or play through the fill with the current cell and its chosen source highlighted, then watch the optimal edit path trace back from the corner to give the answer, 3 — the clearest way to *see* why DP turns exponential recursion into a filled grid), a **Gram-Schmidt** orthogonalizer (drag two vectors and watch the process build an orthogonal set — keep u₁ = v₁, then *subtract v₂'s projection onto u₁* so the remainder u₂ is exactly perpendicular, u₂·u₁ = 0, with a right-angle marker proving it; toggle "normalize" to snap to the orthonormal basis ê₁, ê₂ on the unit circle), and a **Dijkstra's Shortest Paths** visualizer (watch the algorithm grow a shortest-path tree from a source on a weighted graph — each step settles the closest unsettled node, then *relaxes* its edges, lowering a neighbour's tentative distance only when a shorter route appears; tentative distances tick down live, the settled tree edges light up sage, and stepping/playing through it makes the greedy "always expand the nearest frontier" invariant tangible), and a **Policy Gradient (REINFORCE)** visualizer (a one-step softmax policy over three actions with fixed rewards — step the gradient ascent and watch it raise the probability of every action whose reward beats the current average (positive *advantage*) and lower the rest, so probability concentrates on the best action and the expected-return sparkline climbs toward it — the abstract policy-gradient update made concrete), and a **Determinant-as-signed-area** visualizer (drag the two columns of a 2×2 matrix and watch the parallelogram they span — its area is `|det|`, the fill turns from sage to rust when orientation flips at `det < 0`, and it collapses to a line exactly when `det = 0`, i.e. the matrix is singular; the geometric meaning of the determinant made tangible), and a **Lagrange-multipliers** visualizer (maximize `f(x,y)=x+y` on the unit circle — slide a point around the constraint and watch the level line of `f` sweep with it; the constrained optimum is exactly where the line is *tangent* to the circle, i.e. where ∇f and ∇g point the same way, ∇f = λ∇g — with animated Maximize / Minimize, the "why gradients must be parallel" condition made visual), and a **statistical-power** visualizer (two overlapping bell curves — the null `H₀` and an alternative `H₁` separated by an effect size `d` — with a movable decision threshold; the shaded tails are the false-positive rate `α`, the miss rate `β`, and the `power = 1 − β`, with live numbers, so the α/β/power trade-off behind Type I/II errors becomes something you *see*), and an **SVD "rotate · stretch · rotate"** visualizer (step a unit circle through `A = UΣVᵀ` in three stages — a rotation by `Vᵀ`, an axis-aligned stretch by the singular values `σ₁, σ₂` that turns the circle into an ellipse, then a rotation by `U` — with sliders for the two angles and two singular values; the ellipse's semi-axes *are* the singular values, making "every matrix maps the unit circle to an ellipse" tangible), and a **Law of Large Numbers** visualizer (sample a die or coin over and over and watch the running average wander, then settle onto the true mean `μ` — staying inside a `±2σ/√n` band that visibly shrinks as samples pile up; each draw is random, the average is not), and a **Master Theorem** visualizer (a bar chart of the work at each level of a `T(n) = a·T(n/b) + nᵈ` recursion tree — slide `a, b, d` and the per-level ratio `a/bᵈ` tips the bars root-heavy → flat → leaf-heavy, exactly the three master-theorem cases, naming the resulting `Θ(·)` live), and a **Scaling-laws** visualizer (for a fixed compute budget, loss vs. model size is a U-curve — slide the budget and watch the compute-optimal size `N*` march up the curve, always near the Chinchilla ~20-tokens-per-parameter optimum, with predicted loss and the params/tokens split named live; the visceral "bigger isn't better at fixed compute" lesson), and a **Student's t vs. normal** visualizer (slide the degrees of freedom and watch the t-distribution's fatter tails — shaded beyond ±2, with the live tail probability `P(t>2)` — pull in and rise toward the standard normal, converging by about df = 30; the visceral reason small-sample t-tests need a larger critical value), and an **ε–δ limit** visualizer (the formal definition made visual — pick a tolerance ε as a horizontal band around the limit L and watch the widget find a δ interval around a that keeps the whole curve inside it; shrink ε and δ shrinks too, but one always exists, and the removable-discontinuity preset shows the limit ignoring f(a) entirely), and a **Binary Min-Heap** visualizer (the same data shown as a tree *and* as the contiguous array beneath it — insert a value and watch it sift up, extract the minimum and watch the last leaf sift down, with the root always the smallest element; the array↔tree duality and O(log n) heap operations made tangible), and a **Conditional Expectation** visualizer (scatter Y against X, then slice X into bins — the average Y per slice traces `E[Y|X]`, the curve that best predicts Y from X; crank the noise and the slice-averages still recover the underlying relationship, the exact curve regression estimates), and a **Kruskal's MST** visualizer (step through the greedy minimum-spanning-tree build on a weighted graph — edges taken cheapest-first and added in sage, or rejected in dashed rust when union-find detects they would close a cycle, with the running tree weight climbing to the minimum), and a **Joint Distribution** visualizer (a P(X,Y) heatmap with marginal P(X)/P(Y) bars along the edges — toggle between an independent pair, where the joint is exactly the outer product of its marginals so the independence error is 0, and a correlated one, where mass clusters on the diagonal and the joint no longer factors), and a **Geometric distribution** visualizer (the PMF of trials-until-first-success, P(X=k)=(1−p)^(k−1)p — slide p and watch the bars decay geometrically while the dashed mean line sits at 1/p, with a note on its memorylessness as the discrete twin of the exponential), and a **Newton's method** visualizer (step through root-finding on f(x)=x²−2 — each step draws the tangent at the current guess down to where it crosses the x-axis, the next guess, and the iterates rocket toward √2 with quadratic convergence), and an **Area between curves** visualizer (the region between y=2x and y=x² on [0,2] — slide the right edge and watch the shaded area accumulate as ∫(2x−x²)dx = b²−b³/3, reaching 4/3), and a **Greedy activity-selection** visualizer (five activities on a timeline, taken in finish-time order — step through and watch the earliest-finish-compatible rule select the maximum 3 non-overlapping activities, rejecting the tempting long interval that alone would block everything), and a **Hypothesis-test** visualizer (the standard-normal null with the |z|>1.96 rejection region shaded — slide the observed z and watch the p-value tail area and the reject / fail-to-reject decision update live), and a **Bias-variance tradeoff** visualizer (slide the shrinkage λ and watch bias² rise as variance falls — their sum, the MSE, is U-shaped and minimized at a positive λ, so a slightly biased estimator beats the unbiased one), and a **Critical-point** visualizer (a heatmap of f(x,y) around the origin — switch between a bowl, a dome, and a saddle and read off whether the Hessian's eigenvalue signs make it a minimum, maximum, or saddle), and a **Gradient-descent convergence** visualizer (descend on f(x)=x² with an adjustable learning rate η — watch it crawl when η is small, hit the minimum in one jump at η=0.5, zig-zag inward between 0.5 and 1, and diverge once η≥1). Each is embedded inline in the lesson it illustrates, and embeddable anywhere with `<div data-viz="ID"></div>`. The Lab gallery is **searchable and tracks your progress** — a "N of 77 explored" bar, an *Unexplored* filter, **per-topic filter chips** (jump to just the Linear-Algebra or RL widgets, etc.), and a "✓ explored" marker on each widget you've opened, so the collection is easy to navigate and complete (it feeds the *Full Spectrum* achievement).
- **Spawn a Test (Mastery mode)** — build a custom exam from the 2,368-question bank; scope it to *only what you've completed*, your *weak spots*, a topic, or everything; 5/10/20/40 questions. A **live readout** shows how many questions match the chosen scope (and warns when a scope is too thin to test), so you pick a length that fits. **Mastery mode (default on)** re-queues every wrong answer until you get it right — no casual remembering.
- **Mastery model** — every concept tracks a decaying mastery score (from quizzes, flashcards, tests). Drives per-lesson mastery bars, a mastery distribution, "weak spots", smarter tests, and the Knowledge Map colors. Turns "I read it" into "I know it."
- **Knowledge Constellation** — a radial map of all 148 concepts branching out from a central hub (foundations near the core, advanced toward the rim), cross-topic prerequisites arcing through the middle, nodes colored by mastery; hover to trace dependencies, click to open. Fits-to-width on mobile.
- **Learning paths + per-lesson Connections** — every lesson knows its prerequisites; open a concept's path for the ordered chain from the beginning, and each lecture ends with "Builds on / Leads to" chips to navigate the graph while reading.
- **Code Playground** — run real **Python** (via Pyodide, incl. numpy) and **JavaScript** in the browser; **63 lessons now embed a runnable, self-checking code exercise** right in the lecture — at least one in **every one of the seven subjects** (merge sort, BFS, a binary search tree, union-find, substring search, greedy activity-selection, binary search, bubble sort, bottom-up Fibonacci, **matrix multiplication**, the **determinant as signed area**, the **dot product & orthogonality**, a **numerical derivative** (the secant-slope limit), the **chain rule verified numerically**, a **Riemann sum**, a **neuron's forward pass with ReLU**, one **gradient-descent step**, the **discounted return**, **scaled dot-product attention** weights, **perplexity** from token probabilities, a **2×2 system by Cramer’s rule**, a **Bellman value-iteration update**, **Euler’s method** for an ODE, a **1-D convolution** edge detector, **max-pooling**, **Pearson correlation**, **2×2 eigenvalues** from trace & determinant, a **TD(0) value update**, **sample standard deviation** (Bessel), a **gradient** of a scalar field, **counting sort**, a **z-score**, a **definite integral via the FTC**, **batch normalization**, **hashing** to buckets, a **vector projection**, a **Monte-Carlo value estimate**, the **law of total expectation**, **Gram-Schmidt** orthogonalization, a **z-test statistic**, **greedy policy improvement**, a **confidence-interval margin**, the **Hessian second-derivative test**, **gradient clipping**, **PCA explained variance**, a **directional derivative**, the **standard error of the mean**, **singular values** from AᵀA, **He initialization**, the **REINFORCE gradient**, **scaled dot-product attention**, **L2 regularization**, a **BPE merge step**, **softmax**, a neuron's forward pass, backprop, **expectation & variance** of a discrete random variable, **Bayes' theorem** and the false-alarm trap, the **binomial PMF**, the **discounted return** Gₜ=rₜ+γGₜ₊₁, one **Q-learning update**, the **softmax output head**, and **temperature + greedy decoding**) — edit, **Run**, and get an instant ✓/✗ against the expected output. **Solving one (output matches) earns +15 XP and counts toward the *It Runs!* / *Code Adept* achievements** — so the exercises feed the progression loop, not just a checkmark. Embed anywhere with `<div data-code="javascript" data-expected="...">code</div>`.
- **Glossary** — 138 plain-language definitions of the core terms across all seven topics, searchable (and in ⌘K — picking a term there jumps straight to its definition), with per-topic filter chips. Key terms are also **hover/tap tooltips inline in lessons**, so a definition is one glance away without leaving the page — covering the dense ML/DL vocabulary (cross-entropy, KL divergence, logits, dropout, batch/layer norm, perplexity, maximum likelihood, the Markov property), the LLM stack (positional encoding, KV cache, beam search, nucleus sampling, in-context learning, chain-of-thought, scaling laws, LoRA, quantization, RAG, hallucination, autoregressive), the linear-algebra core (dot product, orthogonality, projection, column/null space, diagonalization, orthonormal basis, positive definite), calculus (continuity, critical & saddle points, directional derivative, Riemann sum, the fundamental theorem), algorithms (binary search, recursion, memoization, BSTs, heaps, topological sort, shortest paths, loop invariants), and reinforcement learning (the discounted return, temporal-difference learning, SARSA, the advantage function, actor-critic, off-policy, experience replay, eligibility traces) — the math in each definition is KaTeX-rendered right in the tooltip.
- **Command palette (⌘K / Ctrl+K)** — relevance-ranked, typo-tolerant (fuzzy) search across concepts, visualizations, pages, glossary terms, references, and all 343 worked examples (a matching example opens its lesson straight on the Examples tab) — **plus full-text search inside every lesson's body**, returning the matching lessons with a highlighted context snippet so you can find *where* you read something, not just which lesson it was. It's also a true **command palette**: open it empty for curated quick actions (resume where you left off, start the Daily Mix, review due cards, spawn a test) and **runnable commands** (toggle the theme, cycle the reading text size, restart the tour, **start a focus timer**) — no navigating to settings. On phones (where ⌘K isn't available) a **🔎 search icon in the top bar** opens it in one tap.
- **Focus timer** — for studying alongside a full-time job, an optional **time-boxed study session** (25 or 50 min, from ⌘K) shows a small persistent countdown pill in the corner — pause or stop anytime — and on completion plays a gentle chime, flashes the browser-tab title (so it reaches you even in another tab), and toasts "Focus session complete." It rides along as you move between lessons; no XP gimmick, just a calm nudge to do one real block.
- **Keyboard shortcuts** — 1–4 to answer any MCQ, Space to flip flashcards (then 1–4 to grade), Enter to advance, **`[` / `]` to step to the previous / next lesson in a unit** (sequential reading without reaching for the footer — guarded so it never fires mid-quiz and never crosses out of the course), arrow keys to roam the Knowledge Map. Press **`?`** (or the sidebar's ⌨ Shortcuts) any time for a full in-app reference.
- **Spaced repetition done right** — SM-2 scheduling with Anki-style interval previews on the grade buttons, a global **Daily Review** that distinguishes cards *due for review* from *new* cards (and caps new-card intake per session), with a forecast (due to review / new this session / due in 7 days / in rotation), and weak concepts that resurface automatically.
- **Bookmarks** — star any lesson (☆/★) to save it; bookmarked lessons surface in a dashboard section for one-tap return.
- **Notebook** — every note you write in a lesson's "My notes" box is gathered into one **`#/notes`** page (newest concepts ordered by the curriculum), each linking back to its lesson — your own words become a personal revision deck. **One-tap "⬇ Export all as Markdown"** downloads the whole notebook as a clean `.md` file (a title + summary, grouped by subject in curriculum order, each note under its lesson heading) so your notes are portable — back them up or carry them into any other tool. Reachable from the dashboard, ⌘K, and each lesson's notes box.
- **Redeem your mistakes** — every MCQ you answer wrong (in any quiz or test) is logged to a persistent deck; one click drills *exactly those questions* in mastery mode (re-queued until each one sticks), and they leave the deck the moment you get them right. A finished test also offers an immediate **"Redrill the N you missed"** button right on the results screen — fix them while they're fresh, no detour. Turn failure into the thing you study. (The "think again until you pass" loop.)
- **Concept of the Day** + **"Continue where you left off"** resume card on the dashboard. The dashboard's **Topics grid** shows each subject's *completion* (lessons done) **and** its *mastery* (% retained, decay-aware) — so "clicked through it" and "actually know it" are never conflated.
- **Daily goal + activity heatmap + streak freezes**; a **14-day consistency strip** on the dashboard (the last two weeks as cells that **pop in left-to-right** on landing (a chronological "your history builds up" wave, reduced-motion safe), today ringed, with a "studied N of the last 14 days · keep your streak alive" nudge right where you land each day, plus a **weekly-momentum line** — "📈 N XP this week · ▲/▼ X% vs last week" — a medium-term horizon between the daily streak and lifetime XP that answers *"was this a good week?"*) paired with a **7-day review forecast** beside it — a little bar chart of how many spaced-repetition cards come *due each of the next seven days* (today's bar in gold, plus a "N due now · M more this week · K later" summary), so the SRS rhythm is visible at a glance and you can plan the week (the bars sweep up on landing, reduced-motion safe); a **recent-test performance trend** on the Progress page (your last tests as score bars with running average & best, so you can watch your exam results climb); **Cheatsheets** (print/PDF); **Placement diagnostic** to skip ahead; **Progress export/import**; **Library** of curated references.
- **Gamification** — XP (with a floating "+XP" reward burst and an animated level ring), 10 named levels (Novice → Polymath) shown as a ranks ladder, a daily streak whose **🔥 flame is alive** — it flickers gently in the header and its glow *intensifies with the streak* (unlit → lit → hot at 7 days → blazing at 30 → inferno at 100), and **flares up the moment a new day extends your run** — **58 achievements** (grouped into themed categories — Lessons, Quizzes, Streaks, Flashcards, Mastery, Levels, Exploration — each with its own unlocked count) with a collection-progress bar, and confetti celebrations on a level-up, the moment you **hit your daily XP goal** (it fires the instant you cross it — mid-quiz, mid-review — not as an afterthought, rewarding the daily habit precisely when it happens), **completing a lesson** (a sage ✓ stamps center-screen with a soft glow and the button pops — a "you did it" beat for the core learning action, escalating to confetti when that lesson also **finishes a whole module**, a "📗 Module complete!" burst — and, bigger still, when it **completes an entire subject**, a "🎓 *Topic* complete!" celebration that fires once per topic, so each of the seven subjects you finish gets its own milestone moment). Each course page shows a **per-module progress bar** (X/N done, turning sage on ✓ complete) so you can see how far through each unit you are at a glance, and each lesson row flags its inline extras with compact icons (🎛️ visualization · 🧩 deeper dive · 💻 code exercise) so the hands-on lessons are easy to spot. A **Personal bests** panel on the Progress page tracks your lifetime records — longest streak (with an "at your peak!" tag when today *is* your record), biggest single-day XP haul, best test score, and total days studied — and beating your longest-ever streak fires a "🏆 New record streak!" celebration, turning the daily habit into a high score to chase. Reward moments that used to pass silently now celebrate themselves: **earning a streak-freeze** at each 7-day milestone ("❄️ Streak freeze earned!"), **beating your best test score** ("🎯 New best test score!"), and **setting a new single-day XP record** ("⚡ Best study day yet!", the instant today's total passes your previous best day) — so all three Personal-Bests records (longest streak, best test, biggest day) celebrate in the moment, not just on a stats page. The Progress page's stats cascade-count up from zero when you land on it, and the **mastery/progress bars sweep up from empty to their value** when you open a course or the Progress page — a small "look how far you've come" flourish (reduced-motion safe). Every view also enters with a **staggered rise cascade** (and the Knowledge Map’s constellation now *draws itself in* on landing — structure, then edges, then nodes, reduced-motion-safe) — sections fade up in sequence (a snappy step, capped on long pages so it never drags) rather than snapping in all at once. Achievements reward the behaviors that actually build knowledge — trying a lecture's no-stakes **Quick Check** (and acing one), exploring the visualizations — and there are long-haul "endgame" capstones — matched to the now-complete codex — to chase across months of study: complete all 100+ lessons, answer 5,000 questions correctly, master 100 concepts, hold a 365-day streak, earn 100,000 XP, ace a flawless 40-question exam, and open every visualization.
- **Accessible** — reduced-motion support, a **high-contrast mode** (an independent toggle in the sidebar / ⌘K command palette that layers on *either* theme — deepening text-to-background separation, brightening hairline borders, and intensifying accent colours so low-contrast tokens clear comfortably, with thicker focus rings and solid-underlined inline links; persisted across visits, `aria-pressed` on the control), focus-visible, ARIA + live regions (including **quiz/Quick-Check feedback** — "Correct ✓ / Not quite" plus the explanation is announced the moment you answer, not left silent), a skip link, **proper modal focus management** (the command palette, tour, level-up, and shortcuts overlays are `role="dialog"`/`aria-modal`, move focus inside on open, **trap Tab within the dialog**, and **restore focus to whatever you were on when it closes** — no more being dumped to the top of the page), **meaningful per-route page titles** (the browser tab / history / screen-reader announcement updates to the actual lesson, course, or page name on every navigation), **SPA focus management** (on every in-app navigation, keyboard/screen-reader focus is moved to the new view's heading — so the page name is announced and focus is never stranded on the link you just left; modal-aware, with no scroll jump or visible focus ring on mouse navigation), an adjustable reading text size, an **in-app reduce-motion toggle** (Settings — minimizes animations and transitions without needing an OS setting; persisted, drives both the JS animations and a CSS guard), keyboard/screen-reader-friendly glossary tooltips, every clickable card (Concept of the Day, course cards, lesson rows, the resume + mistakes CTAs) reachable by Tab and activatable with Enter/Space (`role`/`tabindex`/focus ring), the **Knowledge-Map constellation fully keyboard-navigable** (Tab in, arrow keys to move between the 148 concept nodes with a gold focus ring + live dependency highlight, Enter to open; roving tabindex + per-node ARIA labels), and every interactive visualization labeled for screen readers (canvas `role="img"` + description, named sliders), with **every draggable widget keyboard-operable** (the dot-product, vector-addition, Gram-Schmidt, determinant, and projection canvases are focusable — arrow keys nudge the first vector/point, Shift+arrows the second — so none are mouse-only), and these controls are listed in the **`?` keyboard-shortcuts overlay**.
- **Installable PWA / offline** — add to home screen; app shell + content cached; a graceful "new version available" prompt on updates.
- **First-visit tour**, **two themes** (Ink / Parchment).
- **Tooling** — `node gate.js` validates the whole data layer (no dup ids, MCQ answer ranges, flashcards, `data-viz` ids, prereq ids), render-hazard lints every string (unbalanced/unescaped `$`, raw markdown, **and unbalanced HTML tags** — an unclosed `<details>`/`<b>`/`<div>` that would silently swallow the rest of a lesson, checked on math/code-stripped content so `<` inside `$…$` isn't miscounted) **including all 138 glossary definitions**, flags duplicate glossary terms, and **actually runs every embedded JavaScript code-exercise to confirm its `data-expected` output is correct** (58 verified each run) — so a wrong answer key can't ship. It also checks that any hand-authored `#/lesson/…` cross-link resolves to a real lesson (no dangling links) and that **every achievement is reachable** (each one defined in `store.js` appears in a Hall category, and no category references a non-existent achievement) — so a new achievement can never ship invisible.
- **Zero build step.** Pure HTML/CSS/JS; all progress in your browser's `localStorage`.

This platform is maintained by an autonomous improvement loop (`/improve-atlas`); see `CHANGELOG.md` for the per-iteration history and `ROADMAP.md` for the queue.

## Run it locally

Just open `index.html` in a browser. (Math fonts/quizzes need an internet connection
for the KaTeX + Google Fonts CDNs.)

## Deploy to GitHub Pages

1. Create a repo and push these files (the site lives at the repo root).
2. Repo **Settings → Pages → Build and deployment → Source: "Deploy from a branch"**, branch `main` / `/root`.
3. Visit `https://<you>.github.io/<repo>/`.

No Jekyll config needed — there are no underscore-prefixed folders.

## Add or edit content

All content lives in **`data/courses.js`** — no build step, just edit and reload. Structure:

```js
{
  id: "my-topic", title: "My Topic", icon: "∑", color: "#e0a458",
  blurb: "One-line description shown on the card.",
  modules: [{
    id: "m1", title: "Module name",
    lessons: [{
      id: "unique-lesson-id", title: "Lesson title", minutes: 12,
      content: "HTML string. Use $inline$ and $$display$$ for math.",
      mcq:        [{ q, choices:[...], answer:<0-based index>, explain }],
      flashcards: [{ front, back }],
      homework:   [{ prompt, hint, solution }],
      examples:   [{ title, body, solution }],   // optional Examples tab
      prereqs:    ["other-lesson-id", ...]        // optional cross-topic prerequisites
    }]
  }]
}
```

Every field except `id`/`title` is optional, so partial lessons are fine. Math uses
`$...$` (inline) and `$$...$$` (display). To add a whole new subject, append another
course object to the `window.COURSES` array.

To embed an interactive visualization inside a lesson's `content`, drop in
`<div data-viz="la-linear-transform"></div>` (any id from the Visualization Lab).

### Other data files

- `data/references.js` — `window.REFERENCES` maps each topic id (plus `"general"`) to a
  curated list of `{ title, by, kind, url, note }`. `kind` ∈ video/course/book/paper/article/interactive/reference.
- `js/viz.js` — interactive widgets. Add one with `VIZUtil.register({id, topic, title, blurb}, fn)`;
  `fn(container)` builds the widget using the `VIZUtil` helpers (canvas, sliders, buttons, animation loop).
- Prerequisites: a lesson's learning path is its `prereqs` closure **plus** every lesson before it
  in its own course, so paths work even before you add explicit `prereqs` edges.

## Project layout

```
learn-atlas/
├── index.html          # shell: sidebar, topbar, script tags
├── css/styles.css      # the whole theme
├── js/store.js         # localStorage state: XP, levels, streak, SRS, achievements
├── js/app.js           # SPA router + all views + quiz/flashcard/homework engines
└── data/courses.js     # ← all the learning content (edit this)
```

## Tweak the game feel

- XP awards and level thresholds: top of `js/store.js` (`XP`, `LEVELS`).
- Achievements: `ACHIEVEMENTS` array in `js/store.js`.
- Colors/fonts/spacing: CSS variables at the top of `css/styles.css`.
